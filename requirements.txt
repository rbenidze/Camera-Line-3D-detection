# Generated from the actual imports across src/, tools/ and experiments/.
# Versions are the ones this was developed and tested against
# (Python 3.10.11, Windows, plain pip, no conda).

# Core numerics and imaging, used by every stage
numpy==2.2.6
opencv-python==4.10.0.84
scipy==1.15.3

# Point clouds: src/unproject.py, tools/clean_cloud.py, tools/view_cloud.py
open3d==0.19.0

# Disparity previews and the classical-CV experiment
matplotlib==3.10.7

# MCAP / ROS 2 recording access: everything in tools/ that reads the .mcap
mcap==1.3.1
rosbags==0.11.0

# RF-DETR inference via the hosted Roboflow model: src/detect.py
# Needs the ROBOFLOW_API_KEY environment variable at runtime.
roboflow==1.3.1

# RAFT-Stereo inference: src/disparity.py, src/evaluate.py
# Install the CUDA build separately, it is not on the default PyPI index:
#   pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu118
# torchvision is not imported by this repository but is required by the
# upstream RAFT-Stereo repo that src/disparity.py loads.
torch==2.7.1

# NOT LISTED, because they are not pip packages:
#   raft_stereo, utils  -> modules from the upstream RAFT-Stereo clone, added
#                          to sys.path at runtime. Clone it separately and
#                          apply patches/*.patch:
#                            git clone https://github.com/princeton-vl/RAFT-Stereo
#                            git apply /path/to/patches/*.patch
#                          Point src/disparity.py at it with --raft-stereo or
#                          the RAFT_STEREO_PATH environment variable.
